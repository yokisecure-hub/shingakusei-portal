# 運用ガイド — 小規模オーナー向け A8.net 主軸 + LTV最大化

このポータルを使って、**4部屋規模の個人オーナー** が手間をかけずに収益化するための実務手順です。
ハブ企業(B2B一括取次)はスケール不足で審査・採算が合わないため一旦見送り、**A8.net 等のセルフサービス型 ASP** を本線とし、入居時の単発ではなく **在学4年間まるごと使ってもらう LTV(ライフタイムバリュー)** を狙います。

---

## なぜ A8.net 主軸なのか(意思決定の前提)

| 比較項目 | A8.net 等 (個別 ASP) | ハブ企業 (B2B 一括取次) |
| --- | --- | --- |
| 提携審査 | セルフサービス、サイト規模ほぼ不問 | 法人/管理戸数前提、小規模オーナーは門前払いの可能性大 |
| 単価 | 中抜きなし(光回線 2〜3万円/件 等) | ハブが手数料を控除するためオーナー取り分は低い |
| 入居者の手間 | 各社サイトで申込み | 電話 1 本で済み満足度高い |
| 運用コスト | 完全自動・放置可 | パートナー営業との関係維持が必要 |
| 4部屋規模での適合 | **◎ ノーコスト、年間1件でも黒字** | △ 先方の営業コストに見合わず審査困難 |

→ **結論**: 4部屋規模では A8.net 一択。ハブは 5棟以上に拡大した時点で再検討(`config.json#hub.status = "deferred"` で意思を記録済み)。

---

## 全体フロー(Step 1 → 3)

```
[Step 1]  A8.net 登録 & サイト審査通過
    ↓
[Step 2]  即時提携プログラムから順に提携 → リンクURLを取得 → config.json に反映
    ↓
[Step 3]  チラシ・部屋常設ポスター・メール/LINEで導線設置(在学4年間ずっと)
```

---

## Step 1. A8.net 無料会員登録 & サイト審査通過

### 1-1. 登録手順

1. https://www.a8.net/ → **「無料会員登録」** をクリック
2. メール認証 → 基本情報入力 → **報酬振込先銀行口座** を入力(振込手数料の安いネット銀行推奨)
3. 「サイトを持っている」を選択 → サイト情報を入力

### 1-2. サイト情報の入力内容(コピペ可)

| 項目 | 入力内容 |
| --- | --- |
| サイト名 | **入居者専用・生活支援ポータル** |
| サイトURL | https://yokisecure-hub.github.io/shingakusei-portal/ |
| サイトカテゴリ | **暮らし・生活** または **住宅・不動産** |
| 月間PV | (実数) — 控えめでも記載漏れより誠実 |
| サイト紹介文 | 下記テンプレートを貼付 |

### 1-3. サイト紹介文 / 提携申請メッセージ テンプレート

```
当サイトは、自社で所有・管理している学生専用アパート(4部屋・4年制大学近隣)の
入居者および在校生に向けてのみ配布している、クローズドな生活支援ポータルサイトです。

検索エンジン等による不特定多数への集客は行っていません。
アパートの各部屋内(ブレーカー横や掲示板)や契約書類に専用QRコードを常設しており、
「確実に新生活を始める、または生活している大学生とその親権者」という
極めて確度の高いターゲットのみがアクセスします。

規模は限られますが、学生の入居時のネット回線手配、在学中のクレジットカード作成、
卒業時の引越しなど、ライフイベントに直結した質の高いご案内が可能なため、
貴社プログラムとの提携を申請いたしました。
```

> **小規模であることを隠さない**のがコツ。「冷やかしの1万アクセスより、確実なターゲットの10アクセス」を広告主は好む。

---

## Step 2. プログラム提携とリンク取得

### 2-1. 「即時提携」を最優先で攻める

A8.net のプログラムには、ボタン1つで提携完了する **即時提携** と、広告主担当者が目視する **審査あり** がある。
**まずは「即時提携」で `config.json` を埋め切る** こと。これだけでサイト全体がアフィリエイト化する。

検索手順:
1. ログイン → 上部検索窓に「光回線」「引越し」「クレジットカード」「ウォーターサーバー」等を入力
2. 検索結果ページ左の絞り込みで **「即時提携」にチェック** → 再検索
3. 該当案件で **「提携申請」→「即時提携」ボタン** を押下 → 即座に「提携中」へ

### 2-2. リンク URL の抽出 (最重要・ミス多発ポイント)

提携完了後、「**プログラム管理 → 広告リンク作成**」へ進むと、以下のような HTML タグが表示される:

```html
<a href="https://px.a8.net/svt/ejp?a8mat=ABCDEF&a8ejpredirect=https%3A%2F%2Fwww.example-service.com%2F">
  <img border="0" width="320" height="50" alt="プログラム名"
       src="https://www25.a8.net/svt/bgt?aid=000000000000&wid=001&eno=01&mid=s00000000000000000000&mc=1">
</a>
<img border="0" width="1" height="1" src="https://www10.a8.net/0.gif?a8mat=ABCDEF" alt="">
```

`config.json` の `services[].url` に貼り付けるべきは、**最初の `<a href="..."> ` の中身の URL だけ** です:

```
https://px.a8.net/svt/ejp?a8mat=ABCDEF&a8ejpredirect=https%3A%2F%2Fwww.example-service.com%2F
```

#### やってはいけないこと

| ❌ NG | 何が起きるか |
| --- | --- |
| HTML タグごと `url` に貼る | カードのレイアウトが完全に崩壊 / リンクが機能しない |
| `<img>` の URL を貼る | 計測 1x1 ピクセル画像へ飛ぶだけで成果計上されない |
| `&a8ejpredirect=...` 以降だけを貼る | A8.net のクリック計測を経由しないので報酬が発生しない |

#### 抽出のしかた(視覚的に)

```
<a href=" ① URL はここから ② ここまでを抜き出す " >
                                     ↑ 閉じる " の手前まで
```

> 慣れるまでは、A8 管理画面の「ソース」ではなく「ダイレクトリンク (URLのみ)」ボタンがある案件はそれを使う。
> `<a href="..."` を経由せず、`url` の値そのものが取れる。

### 2-3. config.json への反映 + デプロイ

```powershell
cd C:\Claude\ALL\TenantPortal
# エディタで config.json を開き、対応する services[].url を A8 URL に書き換え
git add config.json
git commit -m "Update: replace GMOとくとくBB光 url with A8 affiliate link"
git push
# 1〜2分後、https://yokisecure-hub.github.io/shingakusei-portal/ に反映
```

複数件を一気に書き換えても OK。1件ずつコミットすれば、後で「どの差し替えで CVR が変わったか」を git blame で追える。

---

## Step 3. 4年間ずっと使われる導線設置(LTV最大化)

入居時1回だけのチラシでは、4部屋×4年制大学なら年間1〜2件しかアクセスが発生しません。
**「入居中ずっと使える」というポジションでポータルを常設** すれば、29サービスが在学4年間ずっと収益機会になります。

### 3-1. 配布物(印刷)

| 配布物 | 配置場所 | 想定 src パラメータ | 用意ファイル |
| --- | --- | --- | --- |
| **A4 チラシ** | 契約書類への同封 | `?src=qr` | `flyer.html` |
| **A4 部屋常設ポスター** | ブレーカー横 / 玄関裏 / 掲示板 | `?src=poster` | `flyer_room.html` |

QR は流入元別に作成しておく:

```powershell
python generate_qr.py https://yokisecure-hub.github.io/shingakusei-portal/ --channel qr      # → qr_qr.png
python generate_qr.py https://yokisecure-hub.github.io/shingakusei-portal/ --channel poster  # → qr_poster.png
python generate_qr.py https://yokisecure-hub.github.io/shingakusei-portal/ --all             # → qr/email/line/web + qr_code.png 全種
```

### 3-2. デジタル

`templates/onboarding_email.md` と `onboarding_line.md` をテンプレに、入居案内時のメール/LINE に1行記載。

### 3-3. ライフサイクル別の打ち出し

`config.json` の各 `services[].lifecycle` で時期別のおすすめが追跡可能(現状はメタデータ、将来UI拡張用):

| `lifecycle` | 対象 | 想定タイミング |
| --- | --- | --- |
| `move-in` | 新入居者 | 入居時(春・秋) |
| `in-school` | 在校生 | 在学中いつでも |
| `junior-senior` | 3〜4年生 | 就活期(秋〜春) |
| `move-in-and-graduation` | 入居/退去どちらでも | 春・卒業期 |
| `graduation` | 卒業予定者 | 4年生1〜3月 |

---

## オプトイン文言ルール(変更なし)

| OK | NG |
| --- | --- |
| 「1回で完了します」 | 「必ずお申込みください」 |
| 「すぐにWi-Fiが使えます」 | 「指定業者ですので変更不可」 |
| 「お申込みは任意です」 | (この文を省略する) |
| 「キャッシュバックの場合があります」 | 「絶対に得をします」 |

景表法・特商法・ステマ規制(2023年〜)に注意。`app.js` が `rel="sponsored"` を自動付与しているため、技術的な対応は完了済み。

---

## 想定収益レンジ(4部屋規模・現実値)

| 想定シナリオ | 年間想定件数 | 年間想定報酬 |
| --- | --- | --- |
| 入居時の回線契約 (1〜2部屋退去/入居) | 1〜2件 | 1〜6万円 |
| 在学中のクレカ・サブスク (4部屋×4年=16人年) | 2〜5件 | 1〜3万円 |
| 卒業時の引越し・買取 (1〜2部屋) | 1〜3件 | 0.5〜2万円 |
| **年間合計** | **4〜10件** | **2.5〜11万円** |

> サーバー代0円(GitHub Pages 無料枠) × A8.net 加盟金0円 → **赤字リスク0**。年間1件でも黒字。

---

## 計測 & 改善

A8.net の管理画面で `utm_medium` 別に絞り込み:

| utm_medium | 流入元 | 改善アクション |
| --- | --- | --- |
| `print-qr` | チラシ(契約時同封) | 文言/QR配置/印刷部数を最適化 |
| `room-poster` | 部屋常設ポスター | 設置場所(ブレーカー横/掲示板)を AB |
| `email` | 入居案内メール | 件名・タイミング |
| `line` | 入居案内LINE | 配信時刻・リッチ化 |
| `web` | 直接アクセス | URL周辺の SEO/物件サイト動線 |

成約率の高いカテゴリを `config.json` の上に持ち上げると CVR が改善できる。

---

## 将来の拡張(参考)

- **5棟以上に拡大したら**: ハブ企業(レプリス / エクスパートナーズ等)へパートナー申請 → ポータルTOP に「電話1本コンシェルジュ」を追加し、ハイブリッド型に
- **複数物件展開**: `config.json` の `portal.property_name` を物件ごとに変えて GitHub Pages を物件別に複製 → utm_campaign で物件別 CVR を測定
- **UI 拡張**: `lifecycle` で時期別タブを切替表示 (春は move-in 強調 / 1月は graduation 強調)

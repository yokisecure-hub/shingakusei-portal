# 運用ガイド — 自動化された代理店スキーム 3ステップ

このポータルを使って、不動産オーナー/管理者が手間をかけずに収益化するための具体的な運用手順です。
個別業者と1件ずつ提携する古いやり方ではなく、**ハブ → 埋込 → オプトイン** の3段階で完結します。

---

## Step 1. 一括取次プラットフォーム(ハブ)への登録

個別の業者と1件ずつ契約する代わりに、**1つのダッシュボードから複数カテゴリ(回線/引越し/電気ガス/サブスク等)の紹介URLを発行できる「ハブ」** へ無料登録します。

### 個人〜小規模オーナー向け(推奨)

| ハブ | 種別 | 強み | URL |
| --- | --- | --- | --- |
| **A8.net** | アフィリエイトASP | 国内最大級、ほぼ全カテゴリを横断発行可 | https://www.a8.net/ |
| **バリューコマース** | アフィリエイトASP | Yahoo!系、大手通信/金融案件に強い | https://www.valuecommerce.ne.jp/ |

### 法人/管理戸数のあるオーナー向け

| ハブ | 種別 | 強み | URL |
| --- | --- | --- | --- |
| **リンクライフ ライフサポート** | 不動産向け一括取次 | 回線+電気ガス一括、入居サポート資料も提供 | https://www.linklife.co.jp/ |

### 登録手順(A8.net の例)

1. https://www.a8.net/ → メディア登録 → 個人/法人を選択
2. サイト情報として **本ポータルの公開URL** を申請(GitHub Pages公開後のURL)
3. 審査(通常数営業日)後、ログインしてプログラム検索 → `config.json` に掲載中のサービスを順次提携申請
4. 承認されたプログラムの紹介URLを発行 → `config.json` の各 `services[].url` に上書きして commit & push

> プログラム検索のキーワード例: 「光回線」「引越し」「電気」「ガス」「家具 サブスク」「学生 サブスク」「楽天銀行」「PayPay銀行」「三井住友カード」「Amazon Prime Student」

---

## Step 2. オンボーディング(入居手続き)への組み込み

「入居者が能動的に探さなくても自然に目に入る」状態を作ります。**紙面 / デジタル の両方** に埋め込むのがコツ。

### 2-1. 紙面 — 契約書類への同封チラシ

賃貸借契約の締結時 / 鍵の引き渡し時に渡す書類束に、QR入りA4チラシを1枚同封します。

```powershell
# 公開URL末尾に ?src=qr が自動付与され、流入元を計測可能
python generate_qr.py https://yokisecure-hub.github.io/shingakusei-portal/ --channel qr
# qr_qr.png と qr_code.png が出力される(flyer.html が参照するのは qr_code.png)
```

`flyer.html` をブラウザで開き、印刷ダイアログから A4 / 余白「なし」または「最小」で出力 → 契約書類束へ同梱。

### 2-2. デジタル — 入居案内メール / LINE

`templates/onboarding_email.md` と `templates/onboarding_line.md` をコピー → `{{...}}` プレースホルダを置換して配信。

| チャネル | URL末尾 | 想定転送先 |
| --- | --- | --- |
| 入居案内メール | `?src=email` | utm_medium=email |
| 公式LINE / 個別チャット | `?src=line` | utm_medium=line |
| チラシQR | `?src=qr` | utm_medium=print-qr |
| 直接アクセス | `?src=web` または無し | utm_medium=web |

`app.js` が `?src=<channel>` を読み取り、各サービスへの遷移URLに `utm_source=tenant-portal & utm_medium=<medium> & utm_campaign=<property_name>` を自動付与するため、ハブ側のレポートで「どの導線が稼いだか」を即時把握可能。

### 2-3. 配信タイミング(推奨)

| タイミング | 媒体 | 目的 |
| --- | --- | --- |
| 契約締結時(対面/郵送) | チラシ(QR) | 第一印象で認知させる |
| 鍵引渡し7日前 | 入居案内メール | 入居前準備のリマインド |
| 鍵引渡し当日 | LINE短縮版 | 「まだの方は」の最終リマインド |

---

## Step 3. オプトイン型のインセンティブ提示

無理な営業はせず、「入居者にとって楽になる」というメリットだけを提示して、入居者の自主的なアクセス(オプトイン)を促します。

### 守るべき文言ルール

| OK | NG |
| --- | --- |
| 「1回で完了します」 | 「必ずお申込みください」 |
| 「すぐにWi-Fiが使えます」 | 「指定業者ですので変更不可」 |
| 「お申込みは任意です」 | (この文を省略する) |
| 「キャッシュバックの場合があります」 | 「絶対に得をします」 |

### ポータル内蔵のオプトインバナー

`config.json` の `optin_banner` セクションで、ポータルTOPに表示する文言を制御できます。

```json
"optin_banner": {
  "enabled": true,
  "title": "新生活インフラ開通サポート",
  "message": "このページからまとめて手配すれば、入居日からすぐにWi-Fiが使えて、手続きも1回で完了します。"
}
```

`enabled: false` にすればバナー非表示にできます。

### 法令・ガイドライン上の注意

- **景品表示法**: 「必ず」「絶対」「No.1」等の優良誤認・有利誤認を招く表現は使わない
- **特商法**: 紹介プログラムである旨を `footer_note` で明示(既定で記載済み)
- **ステマ規制(2023年〜)**: 紹介リンクであることを明確化するため、HTML出力時の `rel="sponsored"` を維持(`app.js` が自動付与)
- **個人情報**: 入居者の連絡先をハブ事業者に共有しない(ハブの計測はクリック先での自主登録ベース)

---

## 計測 & 改善

ハブ(A8.net等)のレポート画面で、`utm_medium` 別に成果(クリック数/成約数/報酬額)を絞り込み:

| utm_medium | 流入元 | 想定改善アクション |
| --- | --- | --- |
| `print-qr` | チラシ | 紙面の文言/QR配置/印刷部数を最適化 |
| `email` | メール | 件名/送信タイミング/CTA配置を AB テスト |
| `line` | LINE | リッチメッセージ化/配信時刻を調整 |
| `web` | 直接 | 公開URL周辺の SEO / 物件サイトからの動線整備 |

成約率の高いカテゴリへの寄せ込み(`config.json` の表示順並び替え)で月次CVRを改善できます。

---

## 想定収益レンジの目安(参考)

| 規模 | 想定月間 入居者 | 想定月次成果報酬(全カテゴリ合算) |
| --- | --- | --- |
| 1棟 10室(年回転30%) | 約 0.3 名 | 数千〜1万円 |
| 5棟 50室(年回転30%) | 約 1.2 名 | 1〜4万円 |
| 管理100室(年回転30%) | 約 2.5 名 | 数万〜十数万円 |

> 報酬単価(例): 光回線 8,000〜30,000円 / 件、引越し 1,500〜4,000円 / 件、電気ガス 2,000〜8,000円 / 件、サブスク類 500〜3,000円 / 件
> 上記は紹介プログラム成約時の典型レンジで、ハブ・キャンペーン・成約条件により変動。

---

## トラブルシューティング

| 症状 | 原因 | 対処 |
| --- | --- | --- |
| `?src=email` が来てるのに utm_medium が web 扱い | `app.js` の `tracking.channels` キーと一致していない | `config.json` の `tracking.channels` に該当キーがあるか確認 |
| ハブの管理画面に成果が出ない | リンクURLが古い(ハブ未経由の公式URLのまま) | `config.json` を全件チェック、ハブ発行URLへ置換 |
| QR読み込み先が `example.com/...` | `generate_qr.py` の引数で実URLを指定し忘れ | `python generate_qr.py https://実URL/ --channel qr` で再生成 |
| ポータルにバナーが出ない | `optin_banner.enabled` が `false` | `true` に変更して commit & push |
